apiVersion: v1
kind: Service
metadata:
  name: myservice
  labels:
    app.kubernetes.io/name: todoapp 
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: todoapp
  ports:
  - port: 8083
    targetPort: 80
