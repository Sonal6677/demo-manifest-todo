apiVersion: v1
kind: Service
metadata:
  name: myservice
  labels:
    app.kubernetes.io/name: todoapp 
spec:
  selector:
    app.kubernetes.io/name: todoapp
  ports:
  - port: 8082
    targetPort: 80
